import React from 'react';
import HeroCard from '../components/HeroCard'; // make sure the path is correct

const About = () => (
  <section className="page about">
    <h2>About Me — Stat Sheet & Origin Story</h2>
    <HeroCard />
  </section>
);

export default About;
